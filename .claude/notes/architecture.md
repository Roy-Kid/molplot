# Architecture

> Blueprint stub. Run `/mol:map` to populate this with the module map, public
> surfaces, and layer roles (consumed by the `librarian` agent at spec time).

Until then, see `CLAUDE.md` for the layout and invariants.
